# Theme packs

SpectraDash theme packs live under `community_themes/<id>/manifest.json`. A manifest may select a layout, palette tokens, icon style, scene style, gauge style, and preview metadata. The bundled manifests are safe examples and a starting point for community contributions.
