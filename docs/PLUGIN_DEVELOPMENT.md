# Plugin Development

The plugin API is provisional until v1.0.0.

Plugins should:

- Declare compatibility
- Avoid direct hardware access unless explicitly required
- Handle missing data gracefully
- Avoid collecting unnecessary personal information
- Document permissions and dependencies
