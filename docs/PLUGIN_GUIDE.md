# Plugin Guide

SpectraDash plugins extend the dashboard without changing the core renderer.

Start with [plugin_sdk/README.md](../plugin_sdk/README.md). A plugin should fail safely, declare its settings, avoid blocking the refresh daemon, and provide clear documentation. Submit plugins through a focused pull request with tests and screenshots.
