# Upgrading SpectraDash

```bash
cd ~/SpectraDash
git pull --ff-only
sudo ./install.sh
```

The installer backs up and preserves the existing configuration. Export a configuration copy from Settings before testing a release candidate.

After upgrading, verify both services and render a software preview before performing a physical refresh.
